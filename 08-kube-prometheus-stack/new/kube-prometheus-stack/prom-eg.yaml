apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: prom-eg
  namespace: monitoring
spec:
  gatewayClassName: eg
  listeners:
    - name: http
      protocol: HTTP
      port: 80
